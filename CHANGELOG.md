## v0.1.1

- Fixes issue where CLI could not be executed globally. A missing shebang (`#!/usr/bin/env node`) was added to the build output, resolving permission and command-not-found errors.

## v0.1.0

- Adds `coolify-deploy` package
